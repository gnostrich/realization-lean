# THE OBJECT — context anchor
2026-07-27. Read this first when re-entering. One page. Update, don't append.

## What we are actually circling (has not changed all session)

A certificate-governed adaptation system for frozen LMs. Two-part anatomy
(settled mid-session, easy to lose — do not collapse back to "external shell"):

- **Controller OUTSIDE**: channel state, memory, criterion, meters —
  member-agnostic, countable, reversible. This is where identity lives.
- **Actuation INSIDE**: overlays written at typed depth sites in the
  residual stream (relative depth ℓ/L = the shared type algebra B).
  Harness-as-field, not harness-as-rejection: bias the terrain of the
  forward pass instead of generate-check-retry. Member-specific,
  ports via fitted linear map per model pair.

Governed by certificates instead of schedules:

1. **Grow** memory rank only on certified structural novelty
   (Hankel residual above noise floor).
2. **Graduate** into weights only on certified stillness
   (pole at boundary, residue frozen) — and only past a holonomy veto.
3. **Port — REWRITTEN 2026-07-27 (Rohan's move: human communication as
   existence proof of channel-mediated transfer; no internal maps needed).**
   Identity = behavioral fingerprint. Transfer = ship the spec, RE-REALIZE
   in the target (teach, don't transplant). Grounded in Kalman: minimal
   realizations unique up to similarity (T1); unobservable content cannot
   transfer through any channel (T2); realizable iff capacity ≥ McMillan
   degree ⇒ transfer cost = McMillan degree, substrate-independent (T3).
   The Hochschild/pair-gap problem was an artifact of the abandoned
   linear-map route — re-realization has no composition defect on-spec.
   ONLY remaining empirical question: off-probe generalization.
   Transfer ladder rungs 3–4 DELETED. XFER = closed pre-check of an
   abandoned route; record and move on.

Everything else — the factor graph, Ω, the skin arm, the pool, the tests,
the Lean tiers — is scaffolding around these three.

## The one falsifier that decides it

**Memory rank flat in usage volume, grows in usage diversity.** (H1)
Not yet run. Nothing this session has touched it. It remains the decision point.

## Layer map (zoom levels, same object)

| layer | artifact | status |
|---|---|---|
| Spec | CHANNEL-SHELL-SPEC.md (saved) | complete, buildable, NOT built |
| Empirical: transfer | EBR-XFER-01, final round running | verdict pending; whatever lands, lands; thread CLOSED after |
| Pure math | Hochschild/amenability vs freeness writeup (in chat) | done as reasoning; nothing to prove empirically |
| Formalization | 3 tiers, below | not started |

## What the pure math settled (so we stop re-deriving it)

- Composite transfer error is FIRST order in map error → "singletons fine,
  pairs fine" was never justified. [proven, classical]
- Errors that are inner derivations cost nothing (conjugation). Whether all
  errors are like that = amenability. Free ⇒ non-amenable ⇒ the two good
  properties (freeness for behavior, amenability for structure) pull opposite.
  They answer DIFFERENT questions: behavior-transfer is the optimistic one
  and the one we need.
- GUARD: no cost floor exists (HH² can vanish). Do not regrow the
  rank–cost conservation law. [proven-negative in ledger]

## Formalization tiers (target = implementation faithfulness, not glory)

- T1 (cheap, do first, Aristotle calibration): defect identity;
  inner-absorption; cache-invariance of the harness math.
- T2 (the real one — protects the CORE object): Kronecker, Ho–Kalman,
  "growth test returns exactly the true rank" in EXACT arithmetic.
  Noisy/thresholded version explicitly NOT claimed.
- T3 (defer): free cumulants, semisimple rigidity. Diagnostic, not load-bearing.
- Protocol: Challenge.lean + STATEMENTS.md (+what-we-do-NOT-claim) +
  comparator + CI. Standing rule.

## Standing policies now in force (project memory)

- ONE ROUND testing: full matrix up front, rules fixed before run, validity
  cell, one-page verdict, CLOSED not pending. No branching. No multi-hour
  tests: smallest model, cache activations, variance-derived n, cost stated
  before launch.
- Max-epiplexity synthesis convention: assemble the whole machine, plain
  English + notation + graph + equations + constants + falsifiers.

## Priority order (INVERTED 2026-07-27, Rohan's call: close the
## formalization of the HCF before further implementing/experimenting)

1. FORMALIZATION CLOSURE (no experiments in the path): Challenge.lean
   frozen+compiling → C1+C3 kernel-accepted (validity + locks) →
   C2 library (Hankel/realization, the real work, weeks, Aristotle) →
   CI + comparator green. Done = all four. Build-work, not testing;
   one-round policy does not bind it, finiteness does.
2. Shell v0 + H1: demoted to whenever/parallel. The conjectures (C-H1,
   off-spec) live OUTSIDE closure as labeled bets, taken or not, ever.
3. B-valued lift: Phase 2, explicit door, unchanged.
4. Pool / cross-model: parked, unchanged.
Correction propagated: "learned non-reversible drift" cell is DEAD
(killed 2026-07-25 sweep: Hwang/LNP/Futami incl. learned version);
Ω's negative-authority jobs here are unaffected imports.

## Drift alarms (things this session almost did wrong; don't repeat)

- Letting a side test (transfer) eat the budget of the core test (H1).
- Reintroducing a cost floor via the commutator story.
- Formalizing the noisy criterion (not a theorem; don't claim it).
- Multi-hour empirics; branching follow-ups; verdictless status reports.
- Collapsing the object back to "external shell only" — the overlay/
  internal-actuation half (this file, top) got dropped once already.
